#!/bin/sh

echo "=========================================================="
echo " Router SSH Hardening Reminder"
echo "=========================================================="
echo "1) Disable SSH in router web UI if you no longer need access."
echo "2) Keep WAN SSH disabled unless strictly necessary."
echo "3) Rotate password if password auth was used."
echo "4) Remove temporary password files in /dev/shm if present."
echo "5) Keep firmware updated and re-run baseline checks periodically."
echo "=========================================================="
